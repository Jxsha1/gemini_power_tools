{
  "name": "gemini-power-tools",
  "type": "module",
  "version": "1.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npx esbuild src/scripts/sidebarEntry.js --bundle --minify --outfile=dist/assets/sidebarEntry.js"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
